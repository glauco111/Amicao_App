import 'package:flutter/material.dart';

class LabScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Laboratório Pet'),
        centerTitle: true,
      ),
    );
  }
}
